# 서울 아파트 실거래 대시보드 — PRD

작성일: 2026-08-12 · 작성: 기획 Opus / 구현 Sonnet

---

## 1. 페르소나

**김실거주 (34세, 서울 거주 직장인, 생애 첫 아파트 매수 준비 중)**

- 예산은 **6억~10억**. 대출 한도를 고려하면 이 범위를 크게 벗어나기 어렵다.
- 실거주 목적이라 투자 수익보다 **"지금 사도 되는 시점인가"**가 가장 궁금하다.
- 부동산 앱의 매물 목록은 많이 봤지만, **가격이 오르는 추세인지 꺾였는지**를 숫자로 확인할 방법이 없어 답답하다.
- 관심 지역이 2~3개 있고, 그 지역이 서울 평균보다 빠르게 오르는지 알고 싶다.

### 핵심 사용자 질문 (우선순위 순)

1. **내 관심 구의 가격이 오르는 중인가?** → 월별 추이를 서울 평균과 겹쳐 본다
2. 지금 이 구는 서울에서 어느 정도 가격대인가? → 구별 평당가 순위
3. 내 예산 6~10억으로 이 구에 갈 수 있나? → 예산·면적 필터
4. 실제로 어떤 단지가 얼마에 팔렸나? → 거래 내역 표 + 단지 검색

---

## 2. 데이터

원본: `seoul-apt-latest.csv` (27MB, 320,141행)

| 항목 | 값 |
|---|---|
| 사용 범위 | `deal_type == "매매"` 만 — **72,459건** |
| 기간 | 2025-07 ~ 2026-06 (12개월) |
| 지역 | 서울 25개 구 전체 (최다 노원구 7,190건 / 최소 종로구 639건) |
| 가격 중앙값 | 95,000만원 (9.5억) |
| 6~10억 비중 | 전체의 33.5% |
| 면적 범위 | 11.7㎡ ~ 317.9㎡ |

컬럼: `contract_ym, contract_date, gu, dong, complex, area_m2, floor, deal_type, price, deposit, monthly_rent, price_per_pyeong`

### 데이터 파이프라인 (필수)

27MB CSV를 브라우저가 직접 읽게 하면 안 된다. 빌드 전에 파이썬 스크립트로 전처리한다.

`scripts/build_data.py` (표준 라이브러리만 사용) → `public/data/` 에 아래 3개 생성:

**`meta.json`** — 구 목록, 월 목록(12개), 동/단지명 사전(문자열 배열), 생성 시각, 전체 건수

**`monthly.json`** — 필터 없는 기본 화면용 사전 집계. 필터를 건드리기 전까지는 이것만으로 렌더링한다.
```
{ "seoul": [{ "ym": "2025-07", "avgPrice": 118000, "avgPyeong": 4200, "count": 4149, "medianPrice": 95000 }, ...],
  "gu": { "노원구": [ ...동일 구조 12개... ], ... } }
```

**`transactions.json`** — 필터링용 원본. 컬럼 지향 배열 + 문자열 사전 인코딩으로 용량을 줄인다.
```
{ "fields": ["gu","dong","complex","date","area","floor","price"],
  "gu": [0,0,1,...],        // meta.gu 인덱스
  "dong": [12,12,40,...],   // meta.dong 인덱스
  "complex": [301,...],     // meta.complex 인덱스
  "date": [0,0,1,...],      // 2025-07-01 기준 경과일수 (정수)
  "area": [396,848,...],    // ㎡ × 10 (정수)
  "floor": [4,2,...],
  "price": [138000,...] }   // 만원
```
- 목표 용량: 원본 JSON 3MB 이하 (GitHub Pages는 gzip 전송되므로 실제 전송량은 1MB 내외)
- 실패 시 대안: 면적 300㎡ 초과 등 극단값 제외로 행 수를 줄이지 말 것. **행은 모두 유지**하고 인코딩으로만 줄인다.

---

## 3. 화면 정의

단일 페이지. 위에서 아래로 4개 블록.

### 3.0 컨트롤 바 (상단 고정)

| 컨트롤 | 기본값 | 비고 |
|---|---|---|
| 관심 구 선택 (단일 select) | `노원구` | 거래량이 가장 많아 차트가 안정적 |
| 예산 범위 (이중 슬라이더) | **60,000 ~ 100,000만원** | 페르소나 예산 6~10억 |
| 면적 범위 (이중 슬라이더) | 전체 (11.7~317.9㎡) | ㎡ 단위, 평 병기 |

- **필터는 4개 블록 전체에 동시 반영된다.** 예산을 좁히면 추이·순위·표가 모두 그 조건의 거래만 반영한다.
- 필터 초기화 버튼 필요.
- 조건에 맞는 거래가 0건인 구간은 차트에서 선을 끊고(null), 표에는 "조건에 맞는 거래가 없습니다" 안내.

### 3.1 요약 지표 카드 (4개, 가로 배열)

선택한 구 + 현재 필터 기준.

1. **평균 거래가** — 억 단위 1자리 (예: `9.4억`)
2. **전월 대비** — `+2.3%` / `-1.1%`, 상승 빨강·하락 파랑이 아니라 **상승 primary·하락 label-alternative**로 (투자 대시보드 아닌 실거주 관점이라 색으로 부추기지 않는다)
3. **거래 건수** — `1,034건`
4. **평당가** — `4,200만원/평`

### 3.2 월별 가격 추이 (메인, 가장 큰 영역)

- 꺾은선 2개: **선택한 구**(primary 굵게) vs **서울 전체 평균**(회색 점선)
- X축 12개월, Y축 평균 거래가(억)
- 툴팁: 해당 월의 평균가·중앙값·거래 건수
- 거래 건수를 배경 막대로 옅게 겹쳐 표시 (표본이 적은 달을 사용자가 알아채도록)
- **12개월은 추세를 논하기엔 짧은 기간이다. 차트 하단에 "12개월 데이터 기준이며 계절성이 반영되지 않았습니다" 캡션을 반드시 넣는다.**

### 3.3 구별 평당가 순위

- 가로 막대그래프 25개, 평당가 내림차순
- 선택한 구의 막대만 primary 색, 나머지는 회색
- 막대 클릭 시 그 구로 선택 전환 (3.1~3.4 전부 갱신)
- 현재 필터가 적용된 값

### 3.4 거래 내역 표 + 검색

- 컬럼: 계약일 / 동 / 단지명 / 면적(㎡, 평) / 층 / 거래금액(만원)
- 기본 정렬: 계약일 최신순. 금액·면적 컬럼 헤더 클릭 정렬 지원
- 단지명·동 검색 입력창 (부분 일치, 디바운스 200ms)
- 기본 50건 표시 + "더 보기" 버튼 (가상 스크롤 불필요)
- 상단에 `조건에 맞는 거래 N건` 표시

---

## 4. 기술 스택

| 항목 | 선택 | 제약 |
|---|---|---|
| 빌드 | **Vite 5.4.x** | Node 18.20.7 환경. **Vite 6/7은 Node 20+ 요구라 사용 금지** |
| 프레임워크 | React 18 + TypeScript | |
| 차트 | **Recharts 2.x** | React 18 호환. d3 직접 사용 금지 |
| 상태 | React 내장 (`useState` / `useMemo`) | 상태 관리 라이브러리 도입 금지 |
| 스타일 | **CSS 변수 + 일반 CSS** | Tailwind·CSS-in-JS 도입 금지 |

### 디자인 — Montage 토큰 재사용

기존 `index.html`의 `:root` 블록에 정의된 Montage 토큰을 **그대로** `src/styles/tokens.css`로 옮겨 쓴다. 값을 새로 만들지 말 것.

- primary: `#0066FF` (다크 `#3385FF`) / 배경 `#FFFFFF`·`#F7F7F8` (다크 `#1B1C1E`·`#0F0F10`)
- 텍스트 `#171719`, 보조 `rgba(55,56,60,0.61)`, 경계선 `#E1E2E4`
- spacing 4·8·12·16·20·24·32·40·56·80px, radius 8·12·16px
- 폰트 Pretendard (CDN), `prefers-color-scheme` 다크 모드 지원
- 카드 = 흰 배경 + 1px 경계선 + radius 16px + 옅은 그림자

숫자는 전부 `font-variant-numeric: tabular-nums`.

### 반응형

- 데스크톱 1200px 컨테이너 / 태블릿 2열 / 모바일 1열
- 모바일에서 지표 카드 2×2, 차트 높이 축소, 표는 가로 스크롤

---

## 5. 배포

**GitHub Pages 고정. 저장소 `mindain/sesac-web`, 현재 설정은 `main` 브랜치 `/ (root)`.**

기존 자기소개 페이지(`index.html`)를 **덮어쓰지 않는다.** 대시보드는 하위 경로에 배포한다.

- Vite `base: '/sesac-web/dashboard/'`
- 빌드 산출물을 저장소의 `dashboard/` 폴더로 출력 (`build.outDir: '../dashboard'`)
- 빌드 결과를 커밋해서 push → 현재 Pages 설정 그대로 자동 반영 (**저장소 설정 변경 불필요**)
- 최종 주소: **https://mindain.github.io/sesac-web/dashboard/**
- 기존 자기소개 페이지 하단에 대시보드로 가는 링크 추가

앱 소스는 `app/` 폴더에 둔다. 즉 `app/` = 소스, `dashboard/` = 빌드 산출물(커밋 대상), `.gitignore`에 `app/node_modules` 추가.

---

## 6. 완료 기준

- [ ] `py scripts/build_data.py` 실행 시 `app/public/data/` 에 3개 JSON 생성, 합계 3MB 이하
- [ ] `npm run build` 가 Node 18에서 경고 없이 성공
- [ ] 기본 화면(노원구, 6~10억)에서 4개 블록이 모두 데이터와 함께 렌더링
- [ ] 예산 슬라이더를 조작하면 4개 블록이 모두 갱신
- [ ] 순위 막대 클릭으로 구 전환 동작
- [ ] 라이트/다크 모드 모두 정상
- [ ] 375px 폭에서 가로 스크롤 없음
- [ ] `https://mindain.github.io/sesac-web/dashboard/` 접속 시 정상 동작
- [ ] `https://mindain.github.io/sesac-web/` 자기소개 페이지가 그대로 살아있음

## 7. 범위 밖 (이번에 하지 않음)

- 전세·월세 데이터 (매매만)
- 지도 시각화 (좌표 데이터 없음)
- 가격 예측·투자 수익률 계산 — 실거주 판단 보조가 목적이며, 12개월 표본으로 예측을 제시하면 오해를 부른다
- 로그인, 즐겨찾기 저장, 서버 API
